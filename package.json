{
	"name": "acquire-feeds",
	"version": "1.0.0",
	"description": "The acquire-feeds package enables projects to trigger and run Acquire supplier feeds, including CSV conversion. Use acquire-ftp to upload to FTP if required.",
	"main": "build/index.js",
	"scripts": {
		"test": "jest",
		"build": "tsc",
		"prepare": "npm run build"
	},
	"author": "Geoff Talbot",
	"license": "MIT",
	"devDependencies": {
		"@types/jest": "^29.5.0",
		"@types/node": "^18.15.11",
		"jest": "^29.5.0",
		"ts-jest": "^29.1.0",
		"typescript": "^5.0.3"
	},
	"dependencies": {
		"axios": "^1.3.5",
		"axios-retry": "^3.4.0"
	}
}
